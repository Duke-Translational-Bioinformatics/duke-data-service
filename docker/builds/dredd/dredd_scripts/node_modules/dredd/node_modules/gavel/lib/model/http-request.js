// Generated by CoffeeScript 1.9.2
var ExpectedHttpRequest, HttpRequest, Validatable, clone,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Validatable = require('../mixins/validatable-http-message').Validatable;

clone = require('clone');

HttpRequest = (function(superClass) {
  extend(HttpRequest, superClass);

  HttpRequest.prototype.resourcesKeys = ['method', 'uri', 'headers', 'body', 'expected'];

  function HttpRequest(resources) {
    var i, len, ref, resourceKey;
    if (resources == null) {
      resources = {};
    }
    ref = this.resourcesKeys;
    for (i = 0, len = ref.length; i < len; i++) {
      resourceKey = ref[i];
      this[resourceKey] = clone(resources[resourceKey], false);
    }
    HttpRequest.__super__.constructor.call(this);
  }

  return HttpRequest;

})(Validatable);

ExpectedHttpRequest = (function() {
  ExpectedHttpRequest.prototype.resourcesKeys = ['method', 'uri', 'headers', 'body', 'headersSchema', 'bodySchema'];

  function ExpectedHttpRequest(resources) {
    var i, len, ref, resourceKey;
    if (resources == null) {
      resources = {};
    }
    ref = this.resourcesKeys;
    for (i = 0, len = ref.length; i < len; i++) {
      resourceKey = ref[i];
      this[resourceKey] = clone(resources[resourceKey], false);
    }
  }

  return ExpectedHttpRequest;

})();

module.exports = {
  HttpRequest: HttpRequest,
  ExpectedHttpRequest: ExpectedHttpRequest
};
