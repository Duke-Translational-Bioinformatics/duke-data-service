// Generated by CoffeeScript 1.10.0
var clone, convertAstMetadata;

clone = require('clone');

convertAstMetadata = function(metadata) {
  var cloned, i, item, len, name, result;
  result = {};
  cloned = clone(metadata);
  if (Array.isArray(cloned)) {
    for (i = 0, len = cloned.length; i < len; i++) {
      item = cloned[i];
      name = item['name'];
      delete item['name'];
      result[name] = item;
    }
  }
  return result;
};

module.exports = convertAstMetadata;
