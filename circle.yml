machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  cache_directories:
    - "docker/circle"
  pre:
#    - ./docker/circle/cache_docker_image.sh dredd latest
    - sudo pip install --upgrade docker-compose==1.8.0
test:
  override:
    - echo

deployment:
 zz_dredd_circle_testing:
   branch: dredd_circle_testing
   commands:
     - ./circle/run_dredd.circle.sh
