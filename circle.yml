machine:

  # Version of ruby to use
  ruby:
    version:
      2.2.2

deployment:
  commands:
    heroku run rake db:migrate --app ‘lit-springs-9176'
    heroku run rake db:seed --app ‘lit-springs-9176’
  development:
    branch: develop
    heroku:
      appname: lit-springs-9176
